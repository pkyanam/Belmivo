import assert from'node:assert/strict';import{readFileSync,readdirSync,existsSync}from'node:fs';import{fileURLToPath}from'node:url';import{join}from'node:path';import{pages}from'./content.mjs';
const out=fileURLToPath(new URL('./public/',import.meta.url));assert.equal(readdirSync(out).filter(f=>f.endsWith('.html')).length,6);let links=0;
for(const page of pages){const html=readFileSync(join(out,page.file),'utf8');assert.equal((html.match(/<h1>/g)||[]).length,1);assert.match(html,/<main id="main">/);assert.match(html,/name="viewport"/);assert.match(html,/<html lang="en">/);assert.doesNotMatch(html,/<script|iframe|search-index|analytics/i);for(const m of html.matchAll(/href="([^"]+)"/g)){const target=m[1];if(target.startsWith('https://'))continue;const[file,anchor]=target.split('#');assert.ok(!file.startsWith('/'));const path=join(out,file||page.file);assert.ok(existsSync(path),target);if(anchor)assert.ok(readFileSync(path,'utf8').includes(`id="${anchor}"`),target);links++;}}
const css=readFileSync(join(out,'styles.css'),'utf8');assert.match(css,/@media/);for(const m of css.matchAll(/#([a-f0-9]{3,6})\b/gi)){const c=m[1];assert.ok(c.length===3?c[0]===c[1]&&c[1]===c[2]:c.slice(0,2)===c.slice(2,4)&&c.slice(2,4)===c.slice(4,6));}
console.log(`Checked six pages, ${links} local links/anchors, responsive monochrome styles, and document landmarks.`);
